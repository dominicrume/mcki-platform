# The Event Engine — content/live/events/

One markdown file = one event. The `apps/live` engine renders whichever
event is marked `status: live`. Add a new event by adding a new file.
Toggle modules (demos, scan_sessions, polls, qanda, live_build, start_here)
per event. Each module is independent — one failing never breaks the others.
