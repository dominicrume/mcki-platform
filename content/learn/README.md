# content/learn

Markdown copy and course material for `apps/learn` (MCKI Learn). One source of
truth — no hardcoded copy in components.

Expected structure as the product grows:
- `positioning.md` — what MCKI Learn is, who it is for
- `courses/<slug>.md` — one file per course (mirrors the `content/live/events`
  pattern: adding a course = adding one markdown file)
- `courses/<slug>/lessons/*.md` — lesson bodies

Nothing here yet. Do not fabricate testimonials, outcomes or accreditation —
use clearly-marked `[ PLACEHOLDER: ... ]` where real content is pending.
